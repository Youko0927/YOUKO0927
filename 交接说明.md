# 网站项目交接说明（About Us 三人小组）

## 一、这是什么

一个纯静态单页网站，主题为 About Us，介绍三人小组。黑白灰线框风格（wireframe），
全站中英双语，目前只差填入图片即可成为完整成品。

- 主文件：`studio_set_draft.html`（唯一需要修改的文件，双击即可用浏览器打开）
- 已部署地址：https://f8546f748ba642a8802b77cba9f65816.app.workbuddy.link
- 页面总数：29 页（2 主页面 + 3 组员个人页 + 24 分类详情页）

## 二、页面结构与交互逻辑

| 层级 | 页面 | 进入方式 |
|---|---|---|
| 主流程 1 | Welcome 欢迎页（WELCOME! 大标题 + 字母升起动效） | 打开即是 |
| 主流程 2 | Team 组员页（三张堆叠卡片 A/B/C，各含圆形头像框 + 3 个标签） | 向下滚动 |
| 子页 A/B/C | 组员个人页（水平滚动分类卡 marquee，悬停浮出分类注释） | 点击第二页组员卡片 |
| 详情页 | 分类详情页（非对称四图排版，编号文本压在图片上层） | 点击个人页的分类卡 |
| 详情页（特例） | 联系方式页：无图片，只有标题 + 电话/QQ/邮箱三栏 | 点击"联系方式"分类卡 |

翻页机制：`position: absolute` + `transform: translateY(±100%)` 全屏滚动；
滚轮/触摸/键盘统一走 `navTarget()` 函数。子页上滚返回来源页，下滚无操作。
底部提示：还能下滑时显示 "Scroll Down"，否则显示 "Scroll Up"。

## 三、关键：数据驱动架构（改文案只需改这里）

打开 `studio_set_draft.html`，在文件末尾 `<script>` 开头有两个配置对象，
**所有组员页和详情页都由这两个数据自动生成**，不要手写重复 HTML：

```js
const MEMBERS = [
  { letter: 'A', zh: '组员一', en: 'Member One' },
  { letter: 'B', zh: '组员二', en: 'Member Two' },
  { letter: 'C', zh: '组员三', en: 'Member Three' },
];
const CATS = [
  { n: '01', zh: '专业技能',   en: 'Skills',  tip: '技能栈占位说明 Skills placeholder' },
  { n: '02', zh: '兴趣爱好',   en: 'Hobbies', tip: '爱好占位说明 Hobbies placeholder' },
  { n: '03', zh: '喜欢的音乐', en: 'Music',   tip: '歌单占位说明 Music placeholder' },
  { n: '04', zh: '联系方式',   en: 'Contact', tip: '联系占位说明 Contact placeholder' },
];
```

- 改姓名 → 改 `MEMBERS`；增删分类 → 改 `CATS`（编号自动重排）
- **重要**：`CATS` 数量变化后，必须同步修改 CSS 里 `@keyframes marquee-scroll`
  的循环位移（按 `--mq-w`/`--mq-g` 变量 × 分类数量计算），否则水平滚动会露空
- `en: 'Contact'` 是联系方式极简页的判定条件，不要改这个英文名

## 四、需要填图的位置

1. **组员卡片头像**：第二页每张卡片内的圆形白框（`.avatar-ring`）
2. **分类详情页图片**：四张白框 `.zhen-card .ph`（编号 01-04 的文本压在图片上层）
3. **组员个人页分类卡**：marquee 里的卡片目前是纯文字卡，可换成图片

## 五、设计规范（请沿用，勿破坏）

- 配色：黑 `--ink` / 灰阶 / 纸白底，只用灰阶，不加彩色
- 第四页（详情页）按 12×12 网格定位，每格 8.33%，所有元素坐标均为 8.33% 整数倍
- 详情页编号文本（01 Skills 等）必须压在图上层（`z-index: 2`）遮挡部分图面
- 画布宽度统一 `max-width: 1200px` 居中，两边留白一致

## 六、继续开发的方式

把本文件夹整个交给对方的 WorkBuddy，然后说：

> 打开这个文件夹里的 studio_set_draft.html，这是一个人小组介绍网站，
> 请先阅读「交接说明.md」了解结构，然后按我的要求继续修改。

对方修改完成后，把 `studio_set_draft.html` 复制为 `index.html` 放进 `deploy/` 目录，
再让 WorkBuddy 部署一次即可更新线上地址。
